

    <div>
        <h1 class="has-text-centered" style="font-size: xx-large; text-decoration:underline;font-weight: bolder;">SAVED
            RECIPES</h1>
    </div>
    <!--Container for the saved recipes-->
    <div class="container">
        <div class="row row-cols-3 justify-content-center">
            {{#each recipes as |recipe|}}
            <div>
            <a href="">
                <img src="./images/placeholder.jpg" class="rounded img-fluid col saved-image" alt="...">
            </a>
            <h5 class="card-title">{{recipe.title}}</h5>
            </div>
            {{/each}}
        </div>

    </div>

    
